# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# vercel
.vercel

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env (no secrets are needed in this project — keep it that way)
.env
.env.*

# vendored AI dev-tooling skill packs (installed locally, not part of the app)
/.agents/
/.claude/skills/ui-ux-pro-max/
/skills-lock.json
